import { Injectable } from '@angular/core';
import { RequestService } from './_request.service';

@Injectable({
    providedIn: 'root'
})
export class SprintRequestService {
    constructor(private request: RequestService) {}

    //
}
